//! Real (query-backed) PostgreSQL catalog.
//!
//! See `README.md` and `AI/CONTEXT.md`.

#![allow(dead_code)]

/// Catalog handle. Implementation lands alongside Phase 4 SQL surface.
pub struct Catalog {
    _placeholder: (),
}
